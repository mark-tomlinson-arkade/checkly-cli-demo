export const defaults = {
  pageUrl: "https://danube-web.shop", // the pageUrl can be replaced by urls from a preview / staging environment.
  playwright: {
    viewportSize: { width: 1920, height: 1080 },
  },
};
